machine:
  services:
    - docker
    
dependencies:
  override:
    - docker info

test:
  override:

deployment:
  staging:
    branch: master
    commands:
      - ./buid-and-deploy-carina.sh $TEST_CLUSTER
