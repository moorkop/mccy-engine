machine:
  pre:
    - sudo curl -L -o /usr/bin/docker 'https://s3-external-1.amazonaws.com/circle-downloads/docker-1.9.1-circleci'
    - sudo chmod 0755 /usr/bin/docker
  services:
    - docker

general:
  branches:
    only:
      - master
      - develop

dependencies:
  override:
    - docker info

test:
  override:
    - echo Skipping inferred test

deployment:
  staging:
    branch: master
    commands:
      - ./build-deploy-carina.sh $TEST_CLUSTER:
         environment:
           LETSENCRYPT_DOMAIN: mccy-staging.itzg.me
  production:
    branch: production
    commands:
      - ./build-deploy-carina.sh $TARGET_CLUSTER:
         environment:
           LETSENCRYPT_DOMAIN: mccy.itzg.me

notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/035e6a1140f00a630984
